{% extends 'base.twig' %}

{% block title %}Library{% endblock %}

{% block content %}
   
<div class="row">
    <div class="col-lg-6 col-md-4 " id="result">
        <table class="table">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Lastname</th>
                    <th>Post</th>
                    <th>Book</th>
                    <th>Author</th>
                </tr>
            </thead>
        {% for people in people %}
            <tr>
                <td>{{ people.p_name }}</td>
                <td>{{ people.p_lastname }}</td>
                <td>{{ people.p_post }}</td>
                <td>
                    {% if people.b_name == NULL %}
                        ---
                    {% endif %}
                    {{ people.b_name }}
                </td>
                <td>
                    {% if people.b_author == NULL %}
                        ---
                    {% endif %}
                    {{ people.b_author }}
                </td>
            </tr>            
       {% endfor %}
       </table>
    </div>              
</div>
    
{% endblock %}
